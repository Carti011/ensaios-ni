INSTRUÇÕES PARA O CLAUDE CODE NO WINDOWS — Validação da Fase 2 (ensaios-ni)
==========================================================================

Arquivo temporário. Quando a validação terminar, o Weslley pede para excluí-lo.

Cole esta mensagem para o Claude Code (ou só diga: "leia o VALIDAR-NO-WINDOWS.txt e execute"):

--------------------------------------------------------------------------
Você está no Windows do Weslley. Vamos VALIDAR a aquisição de dados da Fase 2
(tensão no NI 9205 + strain no NI 9235) usando dispositivos SIMULADOS do NI-MAX.
nidaqmx só roda no Windows — este é o ambiente certo.

Faça, nesta ordem, e me reportando cada passo:

1) Confirme o ambiente:
   - python --version   (precisa ser 3.12+)
   - pip install -e .[hardware]
   - pytest             (esperado: 70 passed)

2) Liste os dispositivos que o NI-MAX expõe:
   python -c "import nidaqmx.system as s; print([d.name for d in s.System.local().devices])"
   Se NÃO aparecer um chassi cDAQ-9184 + dois NI 9205 + um NI 9235 SIMULADOS,
   pare e me peça para criá-los no NI-MAX (Devices and Interfaces > Create New >
   Simulated NI-DAQmx Device). Me diga exatamente quais criar.

3) Monte o config/canais.toml a partir de config/canais.exemplo.toml, trocando os
   nomes dos canais pelos nomes REAIS que apareceram no passo 2 (ex.:
   cDAQ9184-...Mod1/ai0 para tensão e cDAQ9184-...Mod3/ai0 para strain).
   Mantenha pelo menos 1 canal tipo="tensao" e 1 canal tipo="strain".

4) Siga o runbook docs/validacao-windows.md do início ao fim. São 4 validações:
   a) Leitura finita (tensão + strain) -> gera CSV com coluna de tensão e de µε.
   b) Aquisição contínua por duração (--continuo --duracao-s 10) -> encerra sozinho,
      tempo_s contínuo e crescente.
   c) Aquisição contínua encerrada por Ctrl-C -> para limpo, CSV íntegro.
   d) Armadilha do strain: confirmar quarter-bridge, 120 ohm, 2,0 V (NUNCA full-bridge
      350 ohm / 2,5 V). O código já tem teste-guarda; confirme também no NI-MAX/test panel.

5) Para CADA validação reporte: rodou sem erro? CSV com as colunas certas? tempo contínuo?
   Se algo falhar, mostre o erro COMPLETO e proponha a correção (sem aplicar sem eu pedir).

Lembre:
- No SIMULADO o número não é físico (sinal sintético). O critério aqui é: monta a task e
  lê SEM ERRO, com timing e parâmetros corretos, e o contínuo flui e encerra limpo.
  O número físico de verdade fica para o hardware do tio (Fase 4), com o mesmo runbook.
- Não commite, não faça push e não exclua nada sem eu pedir.

No fim, me dê um RESUMO do resultado (o que passou, o que falhou) para eu levar de volta
ao Claude do Mac.
--------------------------------------------------------------------------

O que o Weslley precisa abrir no Windows:
- NI-MAX  -> para confirmar/criar os dispositivos SIMULADOS e ver o test panel.
- VS Code com o Claude Code -> para rodar os comandos acima.
